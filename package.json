{
  "name": "cypress-challenge",
  "version": "1.0.0",
  "description": "Cypress app to test the challenge",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:exclude-last": "cypress run --env grep=@all-except-last",
    "cypress:run:last": "cypress run --env grep=@last"
  },
  "keywords": [
    "Cypress",
    "Test"
  ],
  "author": "JP",
  "license": "ISC",
  "devDependencies": {
    "@cypress/grep": "^4.1.0",
    "cypress": "^12.0.0",
    "cypress-wait-until": "^1.7.2"
  }
}
